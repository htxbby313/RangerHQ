# Hermes Skills

These are MVP scaffolds for Ranger HQ. Hermes owns state, approvals, reports, and notification requests. Unity displays that state. OpenClaw handles external notification delivery.

All skills follow the same safety rule: draft and prepare internally by default; require explicit user approval before external publishing, spending, trading, sending final messages, archiving, deleting, or changing major project state.
